class Heroes {
  String name,
      realname,
      team,
      firstappearance,
      createdby,
      publisher,
      imageurl,
      bio;

  Heroes(this.name, this.realname, this.team, this.firstappearance,
      this.createdby, this.publisher, this.imageurl, this.bio);
}
